---
title: Task 1
date: 2026-03-31 14:58:00
tags: 做题记录
mathjax: true
---

三月做题记录。

## [联合省选 2025 Day 2 B. 岁月](https://qoj.ac/problem/10151)

### Statement

(TBD)

### Solution

{% folding green::Sol %}

首先对零入度强连通分量做 DAG 容斥可以预处理出每个点集的生成子强连通图个数。

对于 C 性质，我们需要对单向连通的生成子图计数，考虑到这等价于有唯一零入度强连通分量，直接钦定零入度强连通分量做二项式反演即可。

对于整个题，由 Kruskal 容易知道可以按照边权分层，升序对每一种边权跑一次 C 性质，在一层结束后此时的连通块会缩成一个新的大点，大点里有什么点是确定的，你需要关心的哪些实际结点处于这个大点的零入度强连通分量中（然后终点不在这个集合的边选不选就不能对连通性产生影响了），好消息是每个大点至少有一个结点是处在其零入度强连通分量中的（否则只能不能贡献合法方案），于是你在 DAG 容斥时枚举大点的集合就记录集合中每个大点哪些结点在其零入度强连通分量上，没有结点被选中的大点就没被选入这个集合。相对于 C 性质你需要额外做到计算出在这一层后恰好以这个大点集合作为零入度强连通分量的方案数（然后缩进新一轮大点中），你发现直接对前面说的过程转置下就好了。然后我们分析下复杂度，虽然有至多 $n-1$ 个有效层（无效层需要跳过），但是我们发现每个集合对只会在至多一层中统计到，因此时间复杂度仍为 $\mathcal{O}(3^n)$。

{% endfolding %}

## [NOI 2022 Day 1 C. 树上邻域数点](https://qoj.ac/problem/4659/statement/zh_cn_ynoi)

### Statement

(TBD)

### Solution

{% folding green::Sol %}

怒写 6.7k 代码通过。

先考虑建立一个静态 Top Tree，具体来说就是一个有 $2n-3$ 个结点的满二叉（非叶子即二度）树，每个叶子对应一条边（作为一个簇），然后非叶子是由两个儿子对应的簇进行题面中的运算得来的，特别的，我们称题面中的点信息为界点，而点集是边信息中作为某条边的端点出现的所有点，那我们要求这里的簇除去界点外不能有向外的连边。

那可以发现 compress 可以用来合并重链上相邻的两端，而 rake 则是拼接轻子树，因此你全局平衡下（重链上只能加权分治，合并轻子树可以写哈夫曼树，当然我们发现加权分治写二分找分割点的话可以均摊到不带 $\log$，因此你对轻子树合并也写序列分治可以使得你的建树复杂度是线性，但是本题我们更希望结点深度和小一点，所以写哈夫曼）就可以做到树深度是 $\mathcal{O}(\log n)$ 的。题外话是今年冬令营 lhx 介绍了一种可以减小最大深度的全局平衡手段，具体可以参考他的讲义。

再说回本题，建立出静态 top tree 后我们考虑模仿长剖求 $k$ 级祖先的思想，先随便从一个端点是 $u$ 的叶子跳到尽可能高使得其父亲簇不被当前邻域覆盖了（或者已经到根了），那由这里簇的性质可知邻域剩下的部分只能由两个界点向外的半邻域构成，且因为是极浅的所以两个半邻域半径都不能超过父亲簇大小，而考虑到这是二叉树所以父亲簇大小和与簇大小和同阶，均为 $O(n\log n)$，预处理下就可以 $1$ 次合并回答询问了，因为你可以预处理时先把整个簇信息接某个半邻域上，这样只需要合并两个半邻域。

如何预处理呢，考虑类似长剖换根，先自下向上处理出簇内半邻域信息，再自上到下转移簇外半邻域，这样复杂度还是 $O(n\log n)$。

可能还需要略微卡卡常，就比如说你可以在 compress 和 rake 的过程中计算出簇直径，然后来减少预处理内邻域时循环上界。

{% endfolding %}

## [湖南省选 2011 Day 2 D. 卡农](https://qoj.ac/problem/16349)

### Statement

(TBD)

### Solution

{% folding green:: Sol %}

对称 FWT 即可。

{% endfolding %}

## [联合省选 2026 Day 1 B. 摩卡串](https://qoj.ac/problem/17302)

### Statement

(TBD)

### Solution

{% folding green:: Sol %}

先考虑如何数一个串中小于 $s$ 的子串个数，你试图扫描线，不妨让一个子串被数到当且仅当经过第一个不同的位置（或者末尾），也就说明你只需要关心 $l$ 是后面还有多少个，以及已经填过的前缀中与 $s$ 某个前缀相等的最长后缀长度，是否 $s$ 已经出现和当前已经确定的小于 $s$ 的子串个数。我们调整下可以证明若存在 $t$ 则最短的 $\lvert t\rvert$ 不超过 $2k$（考虑称满足 $\forall j\leq i,\, t[j,i]\geq s$ 的 $i$ 为关键点，则一定有任意包含关键点的区间不造成贡献，而且 $s$ 的一次出现一定满足除去最后一位均为非关键点，因此极小的方案不能有相邻两个关键点，开头也不能是关键点，而至多 $k$ 个非关键点，因此得证），所以状态数是 $\mathcal{O}(nk^2)$ 的，而且转移只用枚举填 $0$ 还是 $1$，转移边数与点数同阶。

也就是说如果你只是要检验是否存在长度为 $l$ 的串的话你可以做到 $\mathcal{O}(lk^2)$，而且你发现只在意是否有解所以可以变成 $\mathcal{O}(\frac{lk^2}{w})$，构造方案只需要记录下来所有递推结果然后倒着找方案即可，而且你还知道如果有解就存在 $l\leq 2k$。

那你就可以发现转置一下，变成从起点 $(0,?,1,k)$ 走到 $(l,0,0,0)$，直接 bitset 优化维护可达性即可。

{% endfolding %}

## [POI 2023/2024 Stage 3 Day 1 B. Skarb](https://qoj.ac/contest/2230/problem/11479/statement/zh_cn)

### Statement

(TBD)

### Solution

(TBD)

## [Ynoi 2018 五彩斑斓的世界](https://qoj.ac/contest/1574/problem/9031)

### Statement

给定一个长为 $n$ 的整数序列 $a$，$m$ 次操作:

+ 区间将 $>x$ 的数减去 $x$
+ 以及查询区间中 $x$ 个数。

$n\leq 10^6,\;m\leq 5\times 10^5\;0\leq a_i,x\leq 10^5+1$

TL: $8$s

### Solution

{% folding green:: Sol%}

分块，对于每个块对于所有值维护一个并查集，可以做到以最大值减少量次并查集操作的整块修改。

{% endfolding %}

## [CTS 2022 Day 1 A. 普罗霍洛夫卡](https://qoj.ac/contest/963/problem/4405)

### Statement

给定一个序列 $a$，以及 $m$ 次询问。每次询问给定一个区间，查询其所有子区间的权值异或和，一个区间 $[l,r]$ 的权值定义为 $a[l,r]$ 中数的种类数。

$1\leq n,m\leq 4\times 10^5.$

TL: 10s

### Solution

{% folding green:: Sol%}

把每一种数对区间的贡献放在其第一次出现上，因此每个 $i$ 对所有 $pre_i<l\leq i\leq r$ 的区间的权值造成了 $1$ 的贡献，其中 $pre_i$ 是 $a_i$ 上一次出现位置。

<img src="\images\普罗霍洛夫卡.png" alt="扫描线" style="zoom:50%;" />

我们画到 $n\times n$ 矩形上就可以发现影响是一个 3-side 矩形加 $1$，询问是查询子矩形异或和，按照 $r$ 从小到大扫描线，需要支持区间加一，区间历史异或和。

从操作的组合意义上讲我们可以发现对于某一行，$l$ 从大到小的权值不降，而且相邻两项差不超过 $1$。

这个问题看起来就不好合并信息，因此我们认定只能分块，设块长为 $B_1$。

我们坚定这样一个策略：遇到散块就重构块内信息（发生 $\mathcal{O}(m)$ 次），整块我们期望直接调用预处理信息来更新整块历史异或和。

那么我们需要对于每个整块维护对于每个块与每个 $x$ 维护出 $f_x=\bigoplus\limits_{i=0}^{B_1-1}(x+a_i)$，这样当时间戳加一时只需要将整块历史异或和异或上 $x$ 取当前标记时的结果。

为了确保信息量的规模是可以接受的，我们进行定期重构，当加法标记等于 $B_2=2^{\lfloor\frac{\log_2 n}{2}\rfloor}$ 时我们也重构块内信息，这样的重构也只会发生 $\mathcal{O}(\frac{n^2}{B_1B_2})$ 次，同时使得 $x\in[0,B_2)$。

然后再考虑如何重构，由前面的性质（似乎其实不需要单调性）可知一个块内极差小于 $B_1$，然后你有一些标记时刻对单点历史和产生了贡献，因此翻转 $a,x$ 的地位依然相当于维护 $x\in[0,B_1)$ 的 $g_x=\bigoplus\limits_{i=0}^{t-1}(x+a_i)$，其中 $t\leq B_2$。

所以我们令 $B=B_1=B_2$ 就好，接下来只需要解决 $\mathcal{O}(B)$ 进行对于长度不超过 $B$ 的序列 $a_i$ 以及所有 $x\in[0,B)$ 的 $x$ 维护出 $\bigoplus_i(x+a_i)$。

记 $k=\lfloor\frac{\log_2 n}{2}\rfloor$。

对于不低于 $k$ 位的部分，每个 $a_i$ 只有至多两种贡献，且一定是 $x$ 某个前缀全都是第一种，其余全都是第二种，因此容易差分维护。

对于剩下 $k$ 位，我们维护一个加法器即可，容易做到 $\mathcal{O}(\sum\limits_{i=0}^{k-1} 2^i)=\mathcal{O}(2^k)=\mathcal{O}(B)$。

每个区间独立，直接离线下来逐块来做空间就是线性了。

{% endfolding %}

## [Ynoi 2019 美好的每一天~ 不连续的存在](https://qoj.ac/problem/11271)

### Statement

给定一棵 $n$ 个结点的树，以及 $m$ 次区间询问，每次查询区间 $[l,r]$ 的导出子图的连通块点集信息的集合，只支持单点加入集合与撤销某个集合的上一次加入操作，要求 $\mathcal{O}(n\sqrt m\log n)$ 次操作，允许离线。

请在[这里](https://www.luogu.com.cn/problem/U670353)提交。

### Solution

{% folding green:: Sol%}

做一个回滚莫队，你发现每一轮，你的右指针从左往右扫的过程中使用启发式合并，总复杂度还是 $\mathcal{O}(n\log n)$ 的，然后你需要保证一个块内总代价不大，我们可以发现维护了 $(l,r]$ 的信息后插入的 $l$ 的代价一定不大于维护了 $(l,n]$ 的信息后插入 $l$，以这个为权值加权分块即可，权值总和是 $\mathcal{O}(n\log n)$，扫右指针一轮的 $\mathcal{O}(n\log n)$，因此总复杂度为 $\mathcal{O}(n\sqrt m\log n)$。

{% endfolding %}

## [NOI 2022 Day 1 B. 移除石子](https://qoj.ac/problem/4658)

### Statement

有一个长为 $n$ 的自然数序列 $a$，我们称其是好的当且仅当可能在进行若干次以下两种操作后变为全 $0$ 序列：

1. 选择连续一段数，全部减 $1$，要求长度不小于 $3$。
2. 选择一个数，使其减少不少于 $2$。

我们称其是 $k\text{-good}$ 的当且仅当可以给一些数增加并且总共增加 $k$ 后成为好的。

给定 $n,k,\{l_i\},\{r_i\}$，求所有长为 $n$ 且满足 $\forall i\in [1,n],\,l_i\leq a_i\leq r_i$ 的 $a$ 有多少个是 $k\text{-good}$ 的。

$t$ 组多测。

$1\leq t\leq 10,\;1\leq n\leq 10^3,\;0\leq k\leq 100$

### Solution

{% folding green:: Sol%}

考虑事实上操作间可交换，于是要求先 $1$ 后 $2$，也就是目标是通过若干次 $1$ 操作使得序列中每一项要么是 $0$ 要么不小于 $2$。

先做 $k=0$。

手玩下可以发现一定能使得操作的每一段长度均不超过 $5$，否则可以拆分，以及一个区间不会被操作超过两次，不然可以根本不操作它让最后这个区间上每一项都不小于 $2$。而且一个点不会被覆盖太多次，因为一定可以只在其中某个点变成 $0$ 的时候才操作一个区间，然后一个点变成 $0$ 就不能再被覆盖，因此也会限制周围点的选择，随便分析下可以分析到不超过 $4$，因此你也可以把 $a_i$ 对 $6$ 取 $\min$，再瞪一下发现改成 $5$ 也对。还有一个不知道有没有用的东西是有的操作序列如果使得每一位都和另一个操作序列相同或者多减了至少 $2$ 的话，它一定不优于对方。

那这个题看起来就是 DFA 题，干脆就写一发暴搜状态吧，发现才 $179$ 个状态，再跑个暴力最小化就 $18$ 了，瞪一下转移发现可以让 $a_i$ 对 $4$ 取 $\min$ 了。

那你继续去做 $k$ 更大的情况，首先发现增加 $k$ 几乎可以改成增加不超过 $k$，如果增加不超过 $k-2$ 合法的话 $k$ 也一定合法，只需要再记录下最少加多少石子合法以及这个石子数增加 $1$ 是否还合法即可，这样也就 $202$ 种状态。但是这个其实可以优化，你再瞪一下发现 $k\geq 2$ 时 $k-1$ 合法则 $k$ 合法，而 $k=1$ 时需要特判全 $0$ 以及 $n=3$ 且全 $1$ 的情况，所以我们干脆只记最少花多少即可。

然后这里还是可以找出状态的偏序关系，如果一个方案目前增加的更少且本身方案也偏序了的话就可以把另一个删掉，这样直接搜就能跑出来才 $5722$ 个状态，本地暴力跑下自动机最小化就才 $1721$ 个状态，转移边 $1721\times 5$ 条，直接打表打下来交上去就通过了。

{% endfolding %}

## [NOI 2023 Day 1 C. 深搜](https://qoj.ac/problem/6757)

### Statement

给定一张 $n$ 个点 $n-1+m$ 条边的简单无向连通图以及其一棵生成树，还有 $k$ 个关键点，求有多少个生成子图存在从某个关键点出发的某一棵 dfs 生成树是给定的生成树，对 $10^9+7$ 取模。

$n,m\leq 5\times 10^5$

TL: 2s

### Solution

{% folding green:: Sol%}

不难发现如果是判定一个点是否可以以它开始走出这个 dfs 树就是检查是不是没有横叉边。

此时会尝试去想点减边容斥，但很遗憾双不交『子树或子树补』的并之交可以有很多个连通块。

因此只得进行纯粹的子集容斥，钦定非空关键点点集 $S$ 中每个点都可以作为起始点，计算方案并带 $(-1)^{\lvert S\rvert+1}$ 的权。

那你画出来这些有限制点，可以发现如果一个点有三个子树（上方那个子树补也计入）中含有限制点的话，它本身也一定能跑出这个 dfs 树，因为横叉边都被限制掉了。

也就是你先随便定一个点为根，对于每个 $S$ 建出它的虚树，虚树上的点除去虚树根以外都一定进行了限制，虚树根只在其被选中或虚树上度数大于等于 $3$ 时需进行限制（否则应视作两侧关键点直接连起来）。那进一步思考可知这等效于要求多余的边只能在每个虚树边对应的链上或者作为某个没有关键点的子树（上方的子树补需要重新拎起来换根）的前向边。

这些操作都可以摊到 dfn 序上通过线段树进行维护，唯一相对较麻烦的是虚树根未被选中且度数为 $2$ 的情况，那你枚举一侧 dfs 进去把可以选入的边的贡献乘到对应 dfn 区间上即可，然后回溯的时候除回来。那你可以要求枚举到的一侧一定不在重子树，所以复杂度可以分析到 $\mathcal{O}(n\log^2n)$。令人惊讶地是这样直接写暴力无需卡常也能。

然后我就懒得写了，但是口胡下如何做到 $\mathcal{O}(n\log n)$：可以发现刚才提及的一部分中边 $u,v$ 只会挂在 $\operatorname{lca}(u,v)$ 上进行一次贡献，于是你对这些有效端点建出虚树暴力扫就行了，不会写虚树遂摆了。

[My Submission.](https://qoj.ac/submission/2148227)

Update: 其实前面在扯淡，你发现你解决没选定的二度虚树根情况时不需要枚举它一起做，直接离线下来写个换根就是 $\mathcal{O}(n\log n)$ 的，但是我还是懒得重写了。

{% endfolding %}

## [NOI 2025 Day2 C. 绝对防御](https://qoj.ac/problem/13084)

### Statement

有一个长为 $n$ 的 $0,1$ 序列 $a$，$q$ 次单点翻转，查询这 $q+1$ 个状态中每一个 $a$ 上最小的一个 $k$ 使得「你开始拥有 $a_1,a_2,\cdots,a_k$，然后指针 $p=k$，存在方案持续这个过程直到 $p\geq n$：取 $a_{p+1},a_{p+2}$（如果 $p=n-1$ 的话就只取 $a_{n}$），然后从你已有的数中删除一个 $0$ 和一个 $1$，或者删除两个 $0$（**相邻两次删除两个 $0$ 的操作间隔不能小于 $z$**），然后将 $p$ 增加 $2$。」

$1\leq n,q\leq 2\times 10^5,\,z=3$

TL: 4s

### Solution

{% folding green: Sol%}

不难发现发现合法的 $k$ 是一段后缀，因此先考虑如下问题：在前 $i$ 轮获得了 $A_i$ 个 $0$ 与 $B_i$ 个 $1$。

为了方便接下来动态维护，我们期待得到几个代数式的判定。

设你在第 $i$ 轮是否是删除了两个 $0$ 的状态是 $c_i$，$\{c_i\}$ 的前缀和是 $\{C_i\}$。

那么你的限制是：
$$
\begin{aligned}
\forall 1\leq i\leq n,\;&C_{i-1}\leq C_i\\
\forall 0\leq i\leq j\leq i+z\leq n,\;&C_i+1\geq C_j\\
\forall 0\leq i\leq n,\;&A_i-i-C_i\geq0\\
\forall 0\leq i\leq n,\;&B_i-i+C_i\geq0
\end{aligned}
$$
于是考虑差分约束，把后两个式子中的 $C_i$ 替换为 $C_i-C_0$ 就好了，判断是否有解就是检查是否无负环。具体来说我们写成标准型是：
$$
\begin{aligned}
\forall 1\leq i\leq n,\;&C_{i-1}-C_i\leq0\\
\forall 0\leq i\leq j\leq i+z\leq n,\;&C_j-C_i\leq 1\\
\forall 0\leq i\leq n,\;&C_i-C_0\leq A_i-i\\
\forall 0\leq i\leq n,\;&C_0-C_i\leq B_i-i
\end{aligned}
$$


这张图上的负权边只有可能由后两种边提供，都一定经过 $0$，而只经过前两种边的话从 $x$ 走到 $y$ 的距离是 $\max\{0,\lceil\frac{y-x}{z}\rceil\}$。

因此我们只需要保证
$$
\begin{aligned}
\forall 0\leq i\leq n,\;&A_i-i\geq 0\\
\forall 0\leq i\leq n,\;&\lceil\frac{i}{z}\rceil+B_i-i\geq0\\
\forall 0\leq l\leq r\leq n,\;&A_l-l+\lceil\frac{r-l}{z}\rceil+B_r-r\geq 0\\
\forall 0\leq l\leq r\leq n,\;&A_r-r+B_l-l\geq0
\end{aligned}
$$
这里每个式子至多出现一个向上取整，因此可以通过把 $\geq 0$ 改成 $>-1$ 来消掉，然后再两边同时乘 $z$，变为：
$$
\begin{aligned}
\forall 0\leq i\leq n,\;&A_i-i\geq 0\\
\forall 0\leq i\leq n,\;&i+zB_i-zi+z-1\geq0\\
\forall 0\leq l\leq r\leq n,\;&zA_l-(z+1)l+zB_r-(z-1)r+z-1\geq0\\
\forall 0\leq l\leq r\leq n,\;&A_r-r+B_l-l\geq0
\end{aligned}
$$
对 $A,B$ 做区间加也都是可以通过线段树容易维护出最小值的，而且你注意到对于同奇偶的 $k$ 来说 $x+A,y+B$ 的变化只是平移，因此你可以直接线段树上二分来解决，时间复杂度 $\mathcal{O}(n+q\log n)$。

{% endfolding %}

## [白井黑子](https://www.luogu.com.cn/problem/P15108)

### Statement

以给定每个点父亲的形式给出一棵树，你可以访问并修改这个父亲序列以及另一个 $a$ 序列，要求只使用 $\mathcal{O}(1)$ 的额外空间使得 $a$ 序列中恰好存了一个原树的 dfs 序。

这两个序列都只能存储 $[0,n]$ 的整数。

$n\leq 10^7$

TL: 1s

### Solution

{% folding green:: Sol%}

首先发现如果保证父亲结点标号小于自己的话，也就是说拓扑序是 $1\sim n$，我们可以先倒着扫描更新出每个子树大小，再正着扫算出每个结点的 dfs 序编号，然后这时候父亲数组就没用了，直接用它作为临时数组对编号序列求逆得到 dfs 序序列。

但是我们发现只能访问父亲的话甚至难以求出一个拓扑序，所以应当反过来想把儿子存下来，如果这是一个二叉树的话就确实可以只用两个数组存下来，所以左儿子右兄弟表示法下，转为求一棵二叉树的先序遍历（注意这里区分左右）。

最为天马行空的一步是：我们发现我们只关心先序遍历，所以右子树塞到左子树的末尾也是可以的，就是我们沿着根的左链扫下去，扫到一个点把它的右儿子接到它左子树右链末尾，时间复杂度是所有左儿子的右链长度和，显然是线性。

{% endfolding %}

## [JOIST 2023 Day 3 B. Cookies](https://qoj.ac/problem/6339)

### Statement

有 $n$ 种曲奇，第 $i$ 种有 $a_i$ 个，以及一个集合 $B=\{b_1,b_2,\cdots,b_m\}$，判断能不能将其划分进若干个盒子使得每个盒子大小必须是某个 $B_j$，且每个盒子内每种曲奇至多出现一次。如果可以，给出最小化盒子数的构造。

$1\leq m\leq n\leq\sum a_i\leq 1.5\times 10^4$

ML: 1GiB

### Solution

{% folding green:: Sol%}

{% folding blue:: Hofflman 定理%}

> 有 $n$ 个源点和 $m$ 个汇点，第 $i$ 个源点可以提供 $P_i$ 的流量，第 $i$ 个汇点可以接收 $Q_i$ 个流量，第 $i$ 个源点到第 $j$ 个汇点的有向边的流量上限是 $c_{i,j}$，源点能流满当且仅当对于任意源点集合 $S$ 都有 $\sum_{u\in S}P_x\leq\sum_{v=1}^m\min\{\sum_{u\in S}c_{u,v},Q_v\}$。

Pf: 考虑用最大流最小割定理，枚举割掉了超级源点向 $S$ 以外的每个源点的连边，剩下就是对每个汇点决策是直接割掉它到超级汇点的边还是割掉 $S$ 中源点到它的所有连边，这些决策间两两独立所以直接写出来就是右式，然后可以流满当且仅当恰好割掉超级源点到源点的所有连边是最小割，因此得到这个不等式限制。

{% endfolding %}

回到本题，假如我们要求大小为 $b_i$ 的盒子有 $x_i$ 个，那么就相当于 $P_i=a_i,\;c_{i,j}=x_j,\;Q_i=x_ib_i$。

于是我们发现对 $S$ 只需要选 $k=0,1,\cdots,n$ 的每个满足 $\lvert S\rvert=k$ 且使得 $\sum_{u\in S}a_u$ 最大的 $S$ 即可。那么 $\min\{\sum_{u\in S}x_v,x_vb_v\}=\min\{\lvert S\rvert,b_v\}x_v$。

所以我们按照 $b_v$ 降序排序然后背包转移，记录当前扫的 $\sum x_v$ 和 $\sum x_vb_v$，用 bitset 优化即可。

这样看起来时间复杂度是 $\mathcal{O}(\frac{m(\sum a_i)^2}{w})$，但你可以发现扫描了 $b_v\geq k$ 后 $\sum x_v$ 也至多为 $\frac{\sum a_i}{k}$，因此把这一维限制后时间复杂度由调和级数可知是 $\mathcal{O}(\frac{(\sum a_i)^2\log\sum a_i}{w})$。

空间上需要存下来算出的所有 dp 值以构造 $x$ 的方案，这是可以开的下的。

然后得到 $x$ 后你每次拿一个盒子把 $a_i$ 最大的几种饼干各拿走一个就好了。这部分时间复杂度 $\mathcal{O}(\sum a_i)$。

{% endfolding %}

## [JOI Final 2026 Day 1 A. Legendary Dango Eater](https://www.luogu.com.cn/problem/P15939)

### Statement

给定一个 $0,1$ 序列 $a$，由 $n$ 个极长连续段构成，这个序列将以压缩的形式给出。

给定 $k$ 与 $q$ 次询问，每次给定一个区间 $[l,r]$，查询将第 $l$ 段到第 $r$ 段构成的子段划分成若干段后 $1$ 比 $0$ 多至少 $k$ 个的段的数量最大值。

$n,q\leq 5\times 10^5$

### Solution

{% folding green:: Sol%}

把 $0$ 改成 $-1$ 后可以类似最大子段和去做，如果扫到当前位置这一段和 $<0$ 了就直接分出来重开一段，因此就是一个确定过程，具体来说是一个复合函数，直接从左到右对段扫描线，用平衡树维护即可，需要支持分裂、合并、推平和加。

容易做到 $\mathcal{O}((n+q)\log q)$。

{% endfolding %}

## [JOI Final 2026 Day 1 B. Garden 3](https://www.luogu.com.cn/problem/P15940)

### Statement

给定定值 $X$，在一个平面上有 $n$ 次矩形（与坐标轴平行）加（保证加的是正数），查询每一次操作后可以覆盖住所有 $\geq X$ 的位置的与坐标轴平行的矩形的最小面积。

$n\leq 2\times 10^4$

### Solution

{% folding green:: Sol%}

容易发现只需求出 $\geq X$ 的位置中纵坐标的最小值（以及可以通过翻转得到的另外 $3$ 个问题），这也是双向规约的，因为你可以先用两次操作把另外三个边界锁死。

接下来考虑如何解决这个问题，发现分治等思路都无法解决不能用线性性拆开的问题，这提示我们多去考虑最小值的特性。

最小值带来的事情是可以双指针，从上往下扫扫到第 $i$ 行时已知前 $i-1$ 行在第 $j$ 次操作后首次出现了 $\geq X$ 的位置，你就只需要查出来在 $j$ 时刻第 $i$ 行是否有 $\geq X$ 的位置，有的话尝试再删去这一次操作的影响并使 $j\leftarrow j-1$ 继续检查下去，这样 $j$ 这个指针也只会扫 $n$ 次，这些操作都容易配合扫描线线段树实现，可以做到 $\mathcal{O}(n\log n)$。

{% endfolding %}

## [JOI Final 2026 Day 2 A. Casino](https://www.luogu.com.cn/problem/P15942)

### Statement

通信题。

给 $A$ 一个 $[0,2^51)$ 的整数，$A$ 输出一个 $8\times 8$ 的 $0,1$ 矩形，交互库会选择一条只能走右和下的从左上走到右下的路径并给路径上每个格子翻转后再发给 $B$，要求 $B$ 能还原出来。

本题多测，要做 $Q$ 次。

$Q\leq 3\times 10^4$

TL: 5s

### Solution

{% folding green:: Sol%}

显然需要对 $2^64$ 个结点求出一个大小不小于 $2^51$ 的独立集，两个点 $u,v$ 连边当且仅当 $u$ 的状态可以通过异或上两条从左上到右下的不同路径得到 $v$ 的状态。

看起来相当困难，但是我们发现两条路径的异或在每个斜线要么是 $2$ 个 $1$，要么是 $0$ 个 $1$，因此我们直接对 $n=1,2,\cdots,8$ 求所有 $2^n$ 个位掩码中，将异或和恰好有 $2$ 个 $1$ 的状态间连边，跑出最大独立集。这里虽然不是二分图，但是可以直接写个 shuffle 多次每次按顺序求极大独立集就行。我们发现这样只能做到 $2^33$。

那需要精细利用到路径的性质，发现如果两个状态不同，我们一定可以提取出其第一次不同的斜线，那么在走到该斜线前它们的路径应当是重合的，因此这一步只能走到相邻的两个位置，再搜一下独立集发现就恰好答案是 $2^51$，具体来说对于长为 $n$ 的斜线，独立集是 $2^{\max\{1,n-1\}}$。考虑下这是什么意思，翻转相邻两位可以考虑做个前缀异或和，就相当于能对最后一位以外的做单点翻转，因此提取所有前缀和后前 $n-1$ 位恰有偶数个 $1$ 的方案就好了。

出题人是个弱智开了多测，所以你还原方案不能写搜所有路径然后检查，但是考虑你一条路径对于每个斜线恰好一次修改，对于每个斜线标记出哪些位置恰好能在翻转后正确，找出一条这样的位置连成的右下路径就好了。

{% endfolding %}

## [JOI Final 2026 Day 2 C. Teleporter 2](https://www.luogu.com.cn/problem/P15944)

### Statement

给定一张大小为 $n$ 的有向图，其中第 $i$ 个结点用一类边连接第 $i+1$ 个结点，然后还有 $m$ 条边形如 $s$ 以正数 $w$ 的边权有向连接 $t$，保证 $s<t$。断掉若干条边使得任意从 $1$ 走到 $n$ 的路径都只能经过不超过 $k$ 条二类边，最小化断掉边的边权和。

$n,m\leq 10^5\; w\leq 10^9$

TL: 3s

### Solution

{% folding green:: Sol%}

等价于将 $[1,n]$ 划分为不超过 $t=k+1$ 个子段最小化每段内二类边的边权和，因为边权是正数，考虑到单调性，将 $t$ 对 $n$ 取 $\min$ 后可以把不超过改成恰好。

继续利用边权是正数，因此这个显然满足四边形不等式，所以关于 $t$ 凸，可以进行 Alien's Trick，二分一个斜率 $x$，每划分一段就多带来 $x$ 的贡献，直接用线段树维护区间加全局 $\max$ 就好了，时间复杂度 $\mathcal{O}((n+m)\log nw\log n)$。

{% endfolding %}

## [NOI 2025 Day 1 C. 数字树](https://qoj.ac/contest/2315/problem/13081)

### Statement

(TBD)

### Solution

{% folding green:: Sol%}

~~本来该有一期如何用 Static Top Tree + 二进制分组虚树 + 分散层叠 + Finger Search 击落数字树暴力的，大概是我上了无数个火箭毛毛虫使得朴素的 $\mathcal{O}(nd^2)$ 暴力优化到了 $\mathcal{O}(n\log^2(n)\alpha(n)$，但是太难写了而且一定跑不过，我们就不管它了~~

事实上本题可以做到空间 $\mathcal{O}(n)$。

首先我们需要抛弃读完题就想到的维护连通块的思路，不然没法在这个题做到优秀的复杂度，接下来我们打开题解读到这样一个性质：

定义函数 $f(u)$：

+ 当 $u$ 不是叶子： $f(u)=f(l_u)\oplus f(r_u)$，其中 $l_u,r_u$ 为 $u$ 的两个儿子，$\oplus$ 表示集合对称差；
+ 当 $u$ 是叶子：若 $u$ 有颜色 $c$ 则 $f(u)=\{c\}$，否则 $f(u)=\emptyset$。

记 $c=\operatorname{card}\left(\{f(u)\mid u\in V,\,\lvert f(u)\rvert\geq 2\}\right)$，则询问的答案为 $2^{2n-1-c}$。

这个的原因是先随便取出来一组方案，一些 $f(u)$ 相等的结点一定处于某一条路径上（因为路径的交是路径，$f(u)$ 相等的点一定是某些路径的交的子集），如果 $\lvert f(u)\rvert\geq 2$ 的话你需要确保它们只有偶数个被翻转了，因此方案数除以 $2$，而对于 $\lvert f(u)\rvert\leq 1$ 的结点你对它们是否翻转是无限制的。

那我把集合看作 $0,1$ 序列，相当于我有 $4n-1$ 个 $0,1$ 序列然后对于每个 $i$ 询问有多少个不同的长度为 $i$ 的前缀（然后减掉 $i+1$，因为全 $0$ 和前 $i$ 个位置恰在某个位置放 $1$ 的序列都存在），那你先在叶子上建立动态开点线段树，没建立的位置填 $0$，然后在树上做线段树合并从而把儿子的序列异或起来得到父亲的，需要做一个持久化。然后得到每个结点的序列的线段树后，我们直接对每个长度的线段树结点做排序，可以将其视为两个儿子构成的二元组，用计数排序，就可以做到 $\mathcal{O}(n\log n)$ 排序了，然后排好序再通过线段树二分查询相邻两个的 LCP，依此建立出的笛卡尔树即为这些序列的压缩 Trie 树。

这样做的空间是 $\mathcal{O}(n\log n)$ 的，但是主播提出了一种线性空间的做法（相对难写但是跑得很快）：

考虑树上线段树合并的本质是让每个线段树区间只在它的端点构成的虚树上重新处理，于是我们做这样一件事：假如我们要处理出每个结点的序列的线段树上区间 $[l,r)$ 的信息：

+ 如果 $r-l=1$：我们就直接拎出来那两个点以及它们的 LCA 处理下即可，我们约定不在虚树上的点如果有子树中点在虚树上就取那个结点的信息，否则设为全 $0$ 信息；
+ 如果 $r-l>1$：我们将其劈成 $[l,m)$ 和 $[m,r)$，先递归下去处理这两个子树，再建立整个区间所有点的虚树，遵循前文提到的原则我们可以处理出虚树上每个结点的 $[l,m)$ 信息与 $[m,r)$ 信息，然后做计数排序，同时也可以维护出排序相邻的两项的 LCP，具体来说写一个 $\mathcal{O}(n)$ - $\mathcal{O}(1)$ 的 RMQ 即可，这样就不需要再访问线段树上子树内信息只需要记录这一层了。注意我们这里虽然需要会先存下左子树信息再进入右子树递归，但是你注意到你的递归栈上存的子树信息无交，因此这部分空间还是线性的。

轻松拿下最优解第一。

{% endfolding %}

## [ULR #3 B. Again Counting Stars](https://uoj.ac/contest/103/problem/1013)

### Statement

(TBD)

### Solution

{% folding green:: Sol%}

我们容易得到一个 $\mathcal{O}(n^2\log n)$ 的做法：

容易发现对于单个 $p$，即为对包含 $p$ 的整数区间 $[l,r]$ 和满足如下条件的长为 $r-l+1$ 的整数序列 $d$ 计数：

$$
\begin{aligned}
\forall i\in [l,r],\; &a_i+d_{i-l+1}\geq 0\\
\forall i\in [1,r-l],\; &\sum_{j=1}^i d_j>0\\
&\sum_{j=1}^{r-l+1}d_j=0
\end{aligned}
$$
不难发现这相当于是在 $[l,r]$ 这段区间上选择一些位置是正的，一些位置是负的，另外一些为空，使得位置 $l$ 是正的而位置 $r$ 是负的，并构造出若干个二元组 $(x,y)$ 满足 $x<y$ 且位置 $x$ 是正的而位置 $y$ 是负的，且二元组间两两不相交（即不存在 $x_1<x_2<y_1<y_2$），且每个非空的位置都至少被选进某个二元组了，对于每个正的位置 $x$ 若其恰处于 $k$ 个二元组则带来 $\binom{a_x}{k}$ 倍的贡献。

那我们倒着扫，记录还继续用于匹配的负的位置个数，并扫到一个位置尝试以其为 $r$ 加入贡献，用 NTT 优化卷积就容易写出来如下代码对于每个 $i$ 统计出 $l=i$ 的总方案数：

```cpp
for(int i=n;i>=1;--i){
    mint binom=1;h[0]=0;
    for(int j=1;j<=n;++j)h[j]=(binom*=(a[i]-j+1)*inv[j]);
    g=f;
    for(int j=0;j<n;++j)g[j+1]+=f[j];
    reverse(f.begin(),f.end());
    (f=convolution(f,h)).resize(n+1),reverse(f.begin(),f.end());
    for(int j=0;j<n;++j)g[j]+=f[j],g[j+1]+=f[j];
    f=g;
    // 在这里提取 f[0]
    f[0]=0;
    ++f[1];
}
```

转置下可以对每个 $i$ 统计 $r=i$ 的总方案数：

```cpp
for(int i=1;i<=n;++i){
    // 在这里提取 f[1]
    f[0]=1;
    mint binom=1;h[0]=0;
    for(int j=1;j<=n;++j)h[j]=(binom*=(a[i]-j+1)*inv[j]);
    g=f;
    for(int j=0;j<n;++j)g[j]+=f[j+1],f[j]+=f[j+1];
    (f=convolution(f,h)).resize(n+1);
    for(int j=1;j<=n;++j)g[j]+=f[j];
    f=g;
}
```

然后我们做个差分就可以对每个 $p$ 得到答案了。

乐零教了我一个办法优化掉卷积。

第二部分看起来形式更好一点，因此我们仔细观察它，是在做这样一件事：
$$
F_i(x)\leftarrow\left(\frac{1+x}{x}(F_{i-1}(x)-F_{i-1}(0))+1\right)(1+x)^{a_i}
$$
瞪一下发现 $[x^1]F_{i-1}(x)+1=[x^0]F_i(x)$，因此我们只需要知道 $[x^0]F_1(x),F_2(x),\cdots,F_n(x)$，在递推时需要维护 $F_i(x)\bmod x^{n-i+1}$。

那我们需要支持，以$\mathcal{O}(n)$ 的时间复杂度提取常数项、乘以 $(1+x)^k$ 、加 $c$ 以及损失一个精度并除以 $x$（保证可以整除），不妨表示成 $F_i(x)\equiv f(x)(1+x)^a\pmod{x^{n-i+1}}$ ，维护 $f(x)\bmod x^{n-i+1}$，于是提常数项只需要提 $f(x)$ 常数项，乘 $(1+x)^k$ 是直接使得 $a\leftarrow a+k$，加 $c$ 可以利用到 $(1+x)^{-a}$ 存在且可以用 ODE 递推直接对 $f(x)$ 修改。然后对这个做法再做转置就能做第一部分。

本题带来的启示是哪怕需要对于 $l=i$ 求你也应当发现那个差卷积的式子应当转置过来优化，以及对你的式子投以耐心来看看能不能简化，乘以 $(1+x)^k$ 不一定需要换元（本题中要 $\bmod x^n$ 所以不好换元），也可以考虑选择 $f(x)(1+x)^a$ 的形式。

{% endfolding %}

## [NAC 2026 L. Leaking Santa's Secrets](https://qoj.ac/contest/3518/problem/17330/statement/zh_cn)

### Statement

已知正整数 $n$，以及交互库有一个大小为 $n$ 的置换 $f$，每次可以给一个置换 $g$ 可以询问 $f\circ g$ 的置换环数（或报告 $f=g$），请在不超过 $1510$ 次操作内报告出 $f$。

$n\leq 100$

### Solution

{% folding green:: Sol%}

先取一下逆假装其实是 $g\circ f$。

对于一个置换 $p$，交换 $p_u,p_v$ 可以翻转 $p$ 中 $u,v$ 是否处于同一个环内的状态，于是通过前后环数增量看出来交换之前 $u,v$ 是否同处于一个环，我们先问一下初始环数，然后从前往后扫，维持当前前缀两两不在一个连通块，每次二分一下处于哪个连通块（或者都不处于）然后拆掉，这样可以求出来 $f$ 的逆。精细实现二分的话操作次数是 $2+\sum_{i=2}^{n}\lceil\log_2 i\rceil=1507<1510$。

{% endfolding %}

## [NAC 2026 A. Acorn Quarrels](https://qoj.ac/contest/3518/problem/17319/statement/zh_cn)

### Statement

有一个 $n$ 个结点的树，你需要为每个结点附上 $[n,2n-1]$ 之间的整数权，使得在令 $i$ 从 $1$ 到 $n$ 执行如下流程后每个结点的权不变：

1. 扫描 $i$ 在树上的邻点 $j$，其中 $a_j$ 最大的 $j$ 构成集合 $S$
2. 对于每个 $j\in S$，令 $a_j\leftarrow a_j-1$
3. 令 $a_i\leftarrow a_i+\lvert S\rvert$

$n\leq 10^5$

### Solution

{% folding green:: Sol%}

要求点权不变看起来很麻烦，强化条件为要求每条边两种方向各流一次，即扫到 $i$ 时要求所有邻点的 $a_j$ 相等。

考虑到树是二分图，所以你的限制只是在每个颜色内部限制，然后利用到树是无环的性质可以发现你的限制一定不会冲突，用加权并查集维护即可，然后限制是形如一些差之间的关系，而且差最大的也不会超过 $n-1$，于是你把每个限制连通块的最小值调到 $n$ 即可。

{% endfolding %}

## [CF2207 H3. Bowser's Castle (Hard Version)](https://codeforces.com/contest/2207/problem/H3)

### Statement

有 $n$ 个变量构成的 $\min,\max$ 表达式树，保证其中序遍历对于变量编号是升序的，你可以每次指定每个变量的取值得到一个运行后的返回值，你需要使用不超过 $3.75n$ 次确定出表达式树的任意一种等价形式。

### Solution

{% folding green:: Sol%}

1h 会了但是写了 3h，感觉我的代码实现能力已经彻底没救了。

首先可以发现值域限制是无所谓的，因为只关心相对大小，一定可以重排到 $[1,n]$ 中，接下来我们放飞自我下。

其次是这个结构并不是唯一的，具体来说形如 $\max\{a,b,\cdots\}$ 和 $\min\{a,b,\cdots\}$ 都有多种对应的表达式二叉树，因此我们把这些都提升到一起，即允许多叉，要求父亲和儿子的运算不同。

你先询问个 $a_i=i$，得到的结果 $p$ 就是 $\max$ 就走最右边，$\min$ 就走最左边最后走到的叶子位置。

那我们尝试剥叶子：

如果 $p=1$ 的话：根的运算是 $\min$，而且 $1$ 自成一个子树挂在根上；

如果 $p=n$ 的话：根的运算是 $\max$，而且 $n$ 自成一个子树挂在根上；

其余情况：

通过把 $a_i$ 改成 $-\infty$ 和 $+\infty$，我们可以问出来从 $a_i$ 的前驱子树开始走停下的位置，以及从 $a_i$ 的后继子树开始走停下的位置。不妨记为 $x$ 和 $y$。

手玩下可以发现停下的位置刚好是对于一个子树，最靠前的前缀端点使得把这个前缀全推成 $-\infty$ 可以回答出 $-\infty$ ，也是最靠后的后缀端点使得把这个后缀全推成 $+\infty$ 可以回答出 $+\infty$。

于是你就发现你赚大的了，直接把 $a[x,p]$ 改成 $y+1$，检查答案是否变成 $y+1$ 就可以判断出来是先接上前驱还是先接上后缀。

也就是说如果你可以在删掉 $x$ 的树上唯一定位出 $x$ 应该插的位置，因此把 $x$ 删掉（你已经知道先碰前驱还是先碰后继，也就是父亲是 $\max$ 还是 $\min$ 了，改成对应的 $\infty$ 即可）后偏移下下标继续跑这个算法求出来去掉 $x$ 的树，然后根据已知的位置插入 $x$ 即可。

综上时间复杂度是 $\mathcal{O}(n^2)$ 的，询问次数是 $\mathcal{O}(n)$ 的，仔细卡卡能不超过 $2n$ 次，于是你狠狠吃一坨就通过了。

{% endfolding %}

## [AGC044 C. Strange Dance](https://atcoder.jp/contests/agc044/tasks/agc044_c)

### Statement

(TBD)

### Solution

{% folding green:: Sol%}

看起来操作就很难维护。

发现操作种类只有两种，就想到可以四毛子下预处理所有不超过 $B$ 的块，时间复杂度是 $\mathcal{O}(2^B+\frac{T}{B})3^n)$，取 $B=\lceil\log_2\left(\frac{T}{\log_2 T}\right)\rceil$ 即可做到最优 $\mathcal{O}(\frac{T}{\log_2 T}3^n)$。还是无法通过。

再仔细看看操作，看到加一就套路地想到根号分治，每次只有不超过 $3^{N-B}$ 个数在 $B$ 位及以后进位了，直接暴力维护后 $B$ 位的情况，然后 $B$ 位以后打懒标记，进位的暴力修改即可，时间复杂度 $\mathcal{O}((3^B+3^{n-B})T)$，取 $B=\lfloor\frac{n}{2}\lfloor$ 即可做到最优 $\mathcal{O}(T\sqrt3^n)$，常数极小，可以通过。

{% endfolding %}

## [ARC151 E. Not Dyed by Majority (Cubic Graph)](https://atcoder.jp/contests/arc161/tasks/arc161_e)

### Statement

(TBD)

### Solution

{% folding green:: Sol%}

冷知识：三个 $0,1$ 变量至少两个 $1$ 的限制是 2-SAT。

由上可知我们可以用 2-SAT 建模在 $\mathcal{O}(n)$ 的时间内判定是否可以对于一个染色方案构造出来一个方案经过操作后得到它。

然后我们发现一定能构造出一个方案使得没有方案能得到它，因为这个是一个 $[2]^n\rightarrow [2]^n$ 的映射，而且不是单射（因为全 $0$ 的方案只改一个点结果也不会变），所以一定不会是满射。

进一步地，我们发现两个染色方案重合的限制很松，我们取一个结点 $u$，如果 $u$ 相连的三个点都满足它们连的另外两个结点颜色相同的话 $u$ 是什么颜色是无所谓结果的，而这样的方案占比一定有常数占比，因此存在一个 $c>0$ 使得有至少占比为 $c$ 的方案无解，直接随机用刚才说的方法去检查就好了。

{% endfolding %}

## [CF1844 H. Multiple of Three Cycles](https://codeforces.com/contest/1844/problem/H)

### Statement

(TBD)

### Solution

{% folding green:: Sol%}

这个题真神了。

首先考虑如何对初始态计算答案，不妨每次取出来标号最小的以及它的后继和后继的后继，然后计算子问题再把这一组插进去。

再看一下修改带来了什么，相当于我们要查询 $n$ 次 $a,b,c$ 表示有标号的 $a$ 个 $1$，$b$ 个 $2$，$c$ 个 $0$ 串成有向环森林且每个环和是三的倍数的方案数，保证这次查询的 $a,b,c$ 是上一次 $a,b,c$ 合并出来的。

不难发现 $c$ 是不重要的，还是可以类似刚才说的那样在最后插进去，接下来只关心 $c=0$ 的情况。先写下 EGF。

有标号黑白球的有向连通环的 EGF 是 $\ln(\frac{1}{1-x-y})$，你希望要求黑球带 $1$ 权重、白球带 $2$ 权重，只提权重和是 $3$ 的倍数的项。

于是单位根反演下，是 $\ln(\frac{1}{1-x-y})+\ln(\frac{1}{1-\omega x-\omega^2 y})+\ln(\frac{1}{1-\omega^2 x-\omega y})=\ln((1-x^3-y^3-3xy)^{-\frac{1}{3}})$，然后你其实不要求连通，所以再 $\exp$ 下，所求是

$$
[\frac{x^ay^b}{a!b!}](1-x^3-y^3-3xy)^{-\frac{1}{3}}
$$

然后就发现其实不会做了呜呜呜，这个东西很难快速提单项，而且我不会 PDE 呜呜呜。

那只能溯源一下重新考虑组合意义，复活一下最开始的配凑思路，设答案是 $f_{a,b}$。

首先我们先考虑钦定每一步选标号最小的 $1$ 球，然后选下它的下一位并合并，也就是 $f_{a,b}=b(a+b-1)f_{a-1,b-1}+(a-1)f_{a-2,b+1}$，然后也可以对称地得到 $f_{a,b}=a(a+b-1)f_{a-1,b-1}+(b-1)f_{a+1,b-2}$。你这时惊讶地发现你可以做到 $\mathcal{O}(n)$ 提单项了，因为你可以交替地使用这两个递推式，就只需要计算前面的一条斜线路径，每一步都形如跳一个中国象棋的马，长度是 $\mathcal{O}(n)$ 的。

进一步地，模仿这个提单项的思路，你只需要维护 $f(x,y),f(x-1,y-1)$，用这个递推式就可以 $\mathcal{O}(1)$ 地向 $(x-1,y-1),(x-2,y+1),(x+1,y-2)$ 转移，于是可以 $\mathcal{O}(1)$ 回答询问了，瓶颈在于并查集。

{% endfolding %}

## [CF2122 G. Tree Parking](https://codeforces.com/contest/2122/problem/G)

### Statement

(TBD)

### Solution

{% folding green:: Sol%}

来点暴力做法。

首先先对一棵树做一下，设 $s_u$ 是子树 $u$ 的结点数，$f_u$ 表示以 $u$ 为根的子树的方案，那转移就是把几个儿子子树拼一下然后在一个空隙插入一对子树根的括号。写出来转移式就是

$$
f_u=(2s_u-1)!\prod_{v\in\text{children}(u)}\frac{f_v}{(2s_v)!}
$$

于是设 $g_u=\frac{f_u}{(2s_u)!}$

于是有

$$
g_u=\frac{\prod_{v\in\text{children}(u)}g_v}{2s_u}
$$

因此总方案数是 $\frac{(2n)!}{2^n}\prod_{u\in V}\frac{1}{s_u}=(2n-1)!!n!\prod_{u\in V}\frac{1}{s_u}$ 即为拓扑序个数的 $(2n-1)!!$ 倍。

于是接下来要做的是对 $n$ 个结点、以 $1$ 为根且有 $k$ 的叶子的有根树拓扑序计数。如果不限制叶子数的话直接枚举拓扑序然后合法的树计数是 $(n-1)!$，因此就是有 $((n-1)!)^2$ 种（因为限制了以 $1$ 为根，拓扑序也必须以 $1$ 开头）。

然后我们直接对叶子做二项式反演，钦定 $m$ 个叶子，带 $(-1)^{m-k}\binom{m}{k}$ 的系数，然后先算 $n-m$ 的答案再插 $m$ 个叶子进去即可，因此所求即为。

$$
\sum_{m=k}^{n-1}(-1)^{m-k}\binom{m}{k}\binom{n-1}{m}\left((n-m-1)!\right)^2m![x^m]\prod_{i=1}^{n-m}\frac{1}{1-ix}
$$

于是你直接分治 NTT 就可以做到 $\mathcal{O}(n\log^2 n)$ 计算答案了。

但是我们需要更进一步，先尝试能不能直接提系数，这是一个分式，我们直接做分式分解

$$
\begin{aligned}
&\prod_{i=1}^{t}\frac{1}{1-ix}\\
=&\frac{\sum_{i=1}^t\prod_{j\neq i}\frac{i}{i-j}(1-jx)}{\prod_{i=1}^t(1-ix)}\\
=&\sum_{i=1}^t\frac{\prod_{j\neq i}\frac{i}{i-j}}{1-ix}\\
=&\sum_{i=1}^t\frac{(-1)^{t-i}i^{t-1}}{(t-i)!(i-1)!}\frac{1}{1-ix}\\
=&\sum_{i=1}^t\frac{(-1)^{t-i}i^t}{(t-i)!i!}\frac{1}{1-ix}
\end{aligned}
$$

因此

$$
[x^m]\prod_{i=1}^{n-m}\frac{1}{1-ix}=\sum_{i=1}^{n-m}\frac{(-1)^{n-m-i}i^n}{(n-m-i)!i!}
$$

（聪明的读者其实早就该发现这是第二类斯特林数了……）

因此我们做一次卷积就好了，时间复杂度 $\mathcal{O}(n\log n)$。

但是还能进一步优化，直接把这个式子代入进去，所求即为

$$
\begin{aligned}
&\sum_{m=k}^{n-1}(-1)^{m-k}\binom{m}{k}\binom{n-1}{m}\left((n-m-1)!\right)^2m!\sum_{i=1}^{n-m}\frac{(-1)^{n-m-i}i^n}{(n-m-i)!i!}\\
=&\frac{(-1)^{n+k}(n-1)!}{k!}\sum_{m=k}^{n-1}\frac{m!(n-m-1)!}{(m-k)!}\sum_{i=1}^{n-m}\frac{(-1)^ii^n}{(n-m-i)!i!}\\
=&\frac{(-1)^{n+k}(n-1)!}{k!}\sum_{i=1}^{n-k}\frac{(-1)^ii^n}{i!}\sum_{m=k}^{n-i}\frac{m!(n-m-1)!}{(n-m-i)!(m-k)!}\\
=&(-1)^{n+k}(n-1)!\sum_{i=1}^{n-k}(-1)^ii^{n-1}\sum_{m=k}^{n-i}\binom{m}{k}\binom{n-m-1}{i-1}\\
=&(-1)^{n+k}(n-1)!\sum_{i=1}^{n-k}(-1)^ii^{n-1}\binom{n}{i+k}
\end{aligned}
$$

于是就可以 $\mathcal{O}(n)$ 计算了，别忘了再乘上 $(2n-1)!!$。

{% endfolding %}

## [CF2164 F2. Chain Prefix Rank (Hard Version)](https://codeforces.com/contest/2164/problem/F2)

### Statement

(TBD)

### Solution

{% folding green:: Sol%}

首先发现限制相当于给定一个点在根链上某个位置，于是写个平衡树就可以快速建出来一张图使得每个结点与祖先恰有两条连边（一条入边，一条出边，我们额外添加两个哨兵 $-\infty,+\infty$ 结点表示根链的开头和结尾）。

建出来的图其实看起来就很锤子，我试了下逐点容斥发现完全做不了（因为怎么都难以容到一个树形结构上），那就考虑最朴素的思路，剥叶子，可以发现叶子一定恰好是二度的，一个入边一个出边，于是你就想到类似广义串并联图，只有 $-\infty,+\infty$ 可能是一度点而你的目标状态是只剩它俩，因此你只需要解决缩二度点和叠重边，由结构可知在删空前总有二度点（叶子一定是），所以做完这些就缩到 $-\infty$ 连 $+\infty$ 的状态了。

那你推广下边的定义，认为是一个序列，有大小以及方案数，叠重边就是两个序列归并，缩二度点就是必须是拼接，实现下就好了，时间复杂度 $\mathcal{O}(n\log n)$，但是其实跑起来常数很大，测了下主要是 map 维护边表比较慢，不知道有没有更好的处理方式？注意这里边带信息，所以可能不太好直接维护端点异或和之类的？如果有好哥哥有什么比较好的方法可以教下我吗/kel/kel/kel

{% endfolding %}